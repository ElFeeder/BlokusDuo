int read_byte = 0;

void setup() {

  Serial.begin(115200); // open serial port with bit rate of 115200
  
}

void loop() {
  
  if (Serial.available() == 0)
  {
     read_byte = Serial.read();
     if (read_byte == '0')        // Name query
       Serial.println("1NE");
     
     
  }
}
